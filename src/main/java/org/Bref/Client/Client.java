package org.Bref.Client;

import org.Bref.ClientGraph.ClientWindow;
import org.Bref.Tcp.TcpClient;

public class Client {
    public Client() {
        /*TcpClient tcpClient = new TcpClient();
        tcpClient.connect("127.0.0.1", 9600);*/
        ClientWindow clientWindow = new ClientWindow();
    }
}
